# Order Management System (OrderMS)

Система управления заказами с графическим интерфейсом, разработанная на C++20 с использованием Qt6.

## Описание задания

Курсовая работа по дисциплине "Объектно-ориентированное программирование" (C++), семестр 3, БГУИР.

**Цель проекта**: Разработка десктопного приложения для управления заказами и товарами с использованием объектно-ориентированного подхода и современных технологий C++.

**Требования к проекту**:
- Использование языка C++20
- Применение объектно-ориентированного программирования
- Реализация графического интерфейса пользователя
- Организация кода по принципам чистой архитектуры
- Использование системы контроля качества кода SonarCloud

## Описание

OrderMS — это десктопное приложение для управления заказами и товарами. Система позволяет создавать и редактировать заказы, управлять каталогом товаров, отслеживать статистику и генерировать отчеты.

## Основные возможности

### Управление заказами
- ✅ Создание новых заказов с указанием клиента
- ✅ Добавление товаров в заказ с указанием количества
- ✅ Редактирование существующих заказов
- ✅ Изменение статуса заказа (новый, в процессе, выполнен, отменен)
- ✅ Автоматический расчет общей стоимости заказа
- ✅ Управление складскими остатками при создании/изменении заказов

### Управление товарами
- ✅ Добавление новых товаров с указанием цены и количества на складе
- ✅ Редактирование информации о товарах (название, цена, остаток)
- ✅ Удаление товаров с проверкой использования в активных заказах
- ✅ Отслеживание складских остатков

### Фильтрация и поиск
- 🔍 Фильтрация заказов по клиенту (с автодополнением)
- 🔍 Фильтрация по статусу заказа
- 🔍 Фильтрация по диапазону общей стоимости
- 🔍 Фильтрация по диапазону ID заказа
- 🔍 Фильтрация по дате создания заказа
- 🔍 Комбинирование нескольких фильтров одновременно

### Статистика и аналитика
- 📊 Статистика по заказам:
  - Количество заказов по статусам
  - Общая выручка
- 📊 Статистика по товарам:
  - Товары с низким остатком
  - Товары с высоким остатком
  - Самые дорогие товары
  - Самые дешевые товары
  - Общее количество товаров
  - Общая стоимость товаров на складе

### Отчеты
- 📄 Генерация отчетов в формате CSV
- 📄 Фильтрация данных для отчетов
- 📄 Включение сводной информации в отчеты
- 📄 Автоматическое именование файлов с временными метками

## Технологии

- **Язык программирования**: C++20
- **GUI Framework**: Qt6 (Widgets)
- **Система сборки**: CMake 3.20+
- **Архитектура**: Layered Architecture (Core, Infrastructure, Services, UI)

## Структура проекта

```
courseProject/
├── include/                 # Заголовочные файлы
│   ├── core/               # Бизнес-логика (Order, Product, интерфейсы репозиториев)
│   ├── infrastructure/     # Реализация репозиториев (TxtOrderRepository, TxtProductRepository)
│   ├── services/          # Сервисный слой (OrderService, ProductService, ReportService)
│   ├── ui/                # UI компоненты (MainWindow, диалоги)
│   ├── utils/             # Вспомогательные утилиты
│   └── Errors/            # Кастомные исключения
├── src/                    # Исходные файлы
│   ├── core/              # Реализация бизнес-логики
│   ├── infrastructure/    # Реализация репозиториев
│   ├── services/         # Реализация сервисов
│   └── ui/               # Реализация UI компонентов
├── cmake-build-debug/     # Директория сборки
│   ├── db/               # База данных (текстовые файлы)
│   │   ├── orders.txt    # Хранение заказов
│   │   └── products.txt  # Хранение товаров
│   └── reports/          # Сгенерированные отчеты
└── CMakeLists.txt        # Конфигурация CMake
```

## Требования

### Системные требования
- Linux (протестировано на Ubuntu/Debian)
- CMake версии 3.20 или выше
- Компилятор C++ с поддержкой стандарта C++20 (GCC 10+, Clang 12+)

### Зависимости
- Qt6 (компонент Widgets)
  - Установка на Ubuntu/Debian:
    ```bash
    sudo apt-get install qt6-base-dev
    ```

## Инструкция по запуску

### Предварительные требования

Убедитесь, что установлены все необходимые зависимости (см. раздел "Требования").

### Клонирование репозитория
```bash
git clone <repository-url>
cd courseProject
```

### Сборка проекта через CMake

1. Создайте директорию для сборки:
```bash
mkdir build
cd build
```

2. Запустите CMake для конфигурации проекта:
```bash
cmake ..
```

3. Соберите проект:
```bash
make
```

Или используйте более быструю параллельную сборку:
```bash
make -j$(nproc)
```

### Запуск приложения

После успешной сборки выполните:
```bash
./app
```

Или из директории сборки:
```bash
cd cmake-build-debug
./app
```

**Примечание**: При первом запуске приложение автоматически создаст необходимые директории (`db/` и `reports/`) в директории, где находится исполняемый файл.

## Использование

### Первый запуск
При первом запуске приложение автоматически создаст необходимые директории:
- `db/` — для хранения данных
- `reports/` — для сохранения отчетов

Файлы базы данных (`orders.txt` и `products.txt`) будут созданы автоматически, если их нет.

### Работа с заказами

1. **Создание заказа**:
   - Нажмите кнопку "Добавить заказ"
   - Введите имя клиента
   - Добавьте товары из списка с указанием количества
   - Сохраните заказ

2. **Редактирование заказа**:
   - Дважды кликните на заказ в таблице
   - Измените необходимые поля
   - Сохраните изменения

3. **Фильтрация заказов**:
   - Используйте поля фильтрации в верхней части окна
   - Примените фильтры для поиска нужных заказов
   - Очистите фильтры кнопкой "Очистить фильтры"

### Работа с товарами

1. **Добавление товара**:
   - Перейдите на вкладку "Товары"
   - Нажмите "Добавить товар"
   - Введите название, цену и количество на складе
   - Сохраните товар

2. **Редактирование товара**:
   - Дважды кликните на товар в таблице
   - Измените необходимые поля
   - Сохраните изменения

### Генерация отчетов

1. Нажмите кнопку "Отчет"
2. Выберите параметры отчета:
   - Название отчета
   - Применить текущие фильтры
   - Включить заголовок с фильтрами
   - Включить сводную информацию
3. Нажмите "Сгенерировать"
4. Отчет будет сохранен в директории `reports/` в формате CSV

### Просмотр статистики

Нажмите кнопку "Статистика" для открытия окна с подробной статистикой по заказам и товарам.

## Формат данных

### Формат хранения заказов
Заказы хранятся в текстовом файле `db/orders.txt` в формате:
```
id|client|status|items|total|createdAt
```

Где `items` — это строка вида `item1:qty1,item2:qty2,...`

### Формат хранения товаров
Товары хранятся в текстовом файле `db/products.txt` в формате:
```
name|price|stock
```

## Архитектура

Проект следует принципам чистой архитектуры с разделением на слои:

1. **Core Layer** — доменные модели (`Order`, `Product`) и интерфейсы репозиториев
2. **Infrastructure Layer** — реализация репозиториев для работы с файловой системой
3. **Services Layer** — бизнес-логика приложения
4. **UI Layer** — графический интерфейс пользователя

### Основные компоненты

- **OrderService** — управление заказами, расчет стоимости, управление статусами
- **ProductService** — управление товарами, контроль складских остатков
- **ReportService** — генерация отчетов в формате CSV
- **TxtOrderRepository** / **TxtProductRepository** — персистентность данных

## Разработка

### Добавление новых функций

1. **Новый сервис**: Добавьте класс в `include/services/` и реализацию в `src/services/`
2. **Новый UI компонент**: Добавьте заголовок в `include/ui/` и реализацию в `src/ui/`
3. **Новый репозиторий**: Реализуйте интерфейс из `include/core/IRepository.h` или `include/core/IProductRepository.h`

### Тестирование

Для тестирования функциональности:
1. Запустите приложение
2. Создайте тестовые данные (товары и заказы)
3. Проверьте работу фильтров, статистики и отчетов

## Контроль качества кода

Проект интегрирован с SonarCloud для автоматического анализа качества кода, поиска ошибок и уязвимостей.

**Ссылка на SonarCloud**: [https://sonarcloud.io/project/overview?id=kiryshabutor_OrderCRM](https://sonarcloud.io/project/overview?id=kiryshabutor_OrderCRM)

SonarCloud автоматически анализирует код при каждом коммите и предоставляет метрики качества, покрытие кода тестами и рекомендации по улучшению.

